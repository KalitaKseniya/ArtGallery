@model List<ArtGallery.Models.Admin>

<div class="container row p-0 m-0">
    <div class="col-10">
        <h2 class="text-primary">Добро пожаловать на страницу администраторов</h2>
    </div>
    <div class="col-6">
        <a asp-controller="Admin" asp-action="Create" class="btn btn-primary">
            Добавить администратора
        </a>
    </div>
</div>
<div class="col-12" border p-3 mt-3>
    <form method="post">
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border">
                <thead>
                    <tr class="table-secondary">
                        <th></th>
                        <th>Логин</th>
                        <th>Пароль</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var admin in @Model)
                    {
                        <tr>
                            <td>
                                <div class="text-center">
                                    <div class="w-75 btn-group" role="group">
                                        <a asp-controller="Admin" asp-route-Id="@admin.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                            Редактировать
                                        </a>
                                        <a asp-controller="Admin" asp-route-Id="@admin.Id" asp-action="Delete" onclick="return confirm('Вы уверены, что хотите удалить администратора?')" class="btn btn-danger mx-2">
                                            Удалить
                                        </a>
                                    </div>
                                </div>
                            </td>
                            <td>@admin.Login</td>
                            <td> @admin.Password</td>
                        </tr>

                    }
                </tbody>
            </table>

        }
        else
        {
            <p>В таблице нет администраторов</p>
        }
    </form>
</div>
